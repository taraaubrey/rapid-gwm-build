# TODO
# - how does Simulation know that it needs a discretization module?
# - how does Simulation know what a tdis module is?

sim:
    # runtime_dependancies: #TODO: this is a special list of required minimum packages if this module is built
    #     - gwf
    #     - tdis
    #     - ims
    cmd: 'flopy.mf6.MFSimulation'
    duplicates_allowed: False

tdis: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        simulation: sim 
    # parameters:
    #     period_data: ['nper', 'perlen', 'nstp', 'steady'] #TODO: this is a special list of adjustable parameters which are not explicitly called
    cmd: 'flopy.mf6.ModflowTdis'
    duplicates_allowed: False

ims:
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        simulation: sim 
    cmd: 'flopy.mf6.ModflowIms'
    duplicates_allowed: False


## GWF packages ----------------------
gwf:
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        simulation: sim 
    cmd: 'flopy.mf6.ModflowGwf'
    duplicates_allowed: True
    runtime_dependancies: ['dis', 'ic', 'npf', 'sto'] #TODO: this is a special list of required minimum packages if a gwf module is built

npf: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    cmd: 'flopy.mf6.ModflowGwfnpf'
    duplicates_allowed: True

dis:
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    cmd: 'flopy.mf6.ModflowGwfdis'
    special_kwargs: #TODO these input keys have special functions & update cfg (these have special checks asssociated with them) otherwise just passed to cmd as is
        - model
        - resolution
        - botm
        - idomain 
    duplicates_allowed: False

ic: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    cmd: 'flopy.mf6.ModflowGwfic'
    special_kwargs:
        - model
    duplicates_allowed: False

sto: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    cmd: 'flopy.mf6.ModflowGwfsto'
    special_kwargs:
        - model
    duplicates_allowed: False
## Simple stress packages ----------------------

wel:
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    cmd: 'flopy.mf6.ModflowGwfwel'
    special_kwargs:
        - model
    duplicates_allowed: True


drn: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    special_kwargs: #TODO: this is a special list of adjustable parameters which are not explicitly called
        - model
        - conductivity
        - elevation 
    cmd: 'flopy.mf6.ModflowGwfdrn'
    # special_methods:
    #     stress_period_data: 'get_stress_period_data function'
    duplicates_allowed: True

ghb: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    cmd: 'flopy.mf6.ModflowGwfghb'
    special_kwargs: []
    duplicates_allowed: True

rcha: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf 
    cmd: 'flopy.mf6.ModflowGwfrcha'
    special_kwargs: []
    duplicates_allowed: True


# ??
sfr: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf
    cmd: 'flopy.mf6.ModflowGwfsfr'
    duplicates_allowed: True

oc: 
    build_dependancies: # TODO referencing a module; in the future will need to be some sort of ModuleManager class to get the module
        model: gwf
    cmd: 'flopy.mf6.ModflowGwfoc'
    duplicates_allowed: True